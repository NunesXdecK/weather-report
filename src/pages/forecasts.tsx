import WeatherReports from "../components/weather-reports";

const Forecasts: React.FC = () => {
  // { latitude: 2.811773, longitude: -60.716728, }
  return <WeatherReports />;
};

export default Forecasts;
